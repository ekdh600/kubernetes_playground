{{- if .Values.serviceAccount.create -}}
# ── 플랫폼 ServiceAccount ─────────────────────────────────────────────────
# API 서버 파드의 K8s 신원(identity)을 나타내는 ServiceAccount.
# ClusterRoleBinding을 통해 플레이그라운드 관리에 필요한 클러스터 권한을 가진다.
# API 서버 Deployment의 serviceAccountName으로 참조된다.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name }}  # values.yaml: playground-platform
  namespace: {{ .Release.Namespace }}
{{- end }}
